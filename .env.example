# =============================================================================
# PDF RAG on EKS -- Environment Variables
# Copy this file to .env and fill in values for local development.
# In Kubernetes, these are provided via ConfigMap and Secrets.
# =============================================================================

# --- Qdrant Vector Database ---
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION=pdf_chunks

# --- Embedding Server (HuggingFace TEI) ---
EMBEDDING_URL=http://embedding:8080
EMBEDDING_MODEL=nomic-ai/nomic-embed-text-v1.5
EMBEDDING_DIMENSION=768
EMBEDDING_BATCH_SIZE=32

# --- vLLM Server ---
VLLM_URL=http://vllm:8000
VLLM_MODEL=hugging-quants/Meta-Llama-3.1-8B-Instruct-AWQ-INT4

# --- RAG Pipeline ---
RAG_TOP_K=5
RAG_TEMPERATURE=0.1
RAG_MAX_TOKENS=1024

# --- Chunking ---
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# --- PostgreSQL (conversation history -- deferred) ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=ragchat
POSTGRES_USER=ragchat
POSTGRES_PASSWORD=changeme

# --- PDF Data ---
PDF_DIRECTORY=/data
